# @package hydra
# Timestamped run directories under logs/ (mirrors the GRID parent framework's convention).

run:
  dir: logs/${now:%Y-%m-%d}/${now:%H-%M-%S}
sweep:
  dir: logs/multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
  subdir: ${hydra.job.num}
job:
  chdir: false
output_subdir: hydra
